"""LangGraph wiring — builder, checkpointer, config.

Provides per-clinic graph compilation with configurable nodes,
confidence thresholds, interrupt points, and MemorySaver checkpointing
for persistent multi-turn conversation state.
"""
